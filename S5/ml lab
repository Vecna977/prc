
1.#Load the data
x=[[1,1],[2,3],[8,0],[9,0]]
y=["red","green","red","green"]

2.#load the model
from sklearn.linear_model import Perceptron
model=Perceptron()

3.#fit into the model
model.fit(x,y)

4.#predict...testing
op=model.predict([[3,3]])
print(op)











.#Load the data
import pandas as pd
file=pd.read_csv("/irisexcel.csv")
x=file[["sepal_length","sepal_width","petal_length","petal_width"]]
y=file["species"]

2.#load the model
from sklearn.linear_model import Perceptron
model=Perceptron()

3.#fit into the model
model.fit(x,y)

4.#predict...testing
op=model.predict([[1.9,2.2,3.3,1.1]])
print(op)


1.#Load the data
import pandas as pd
file=pd.read_csv("/irisexcel.csv")
x=file[["sepal_length","sepal_width","petal_length","petal_width"]]
y=file["species"]

2.#cross validation
from sklearn.model_selection import train_test_split
xtrain,xtest,ytrain,ytest=train_test_split(x,y,test_size=0.2)


3.#load the model
from sklearn.linear_model import Perceptron
model=Perceptron()

4.#fit into the model
model.fit(xtrain,ytrain)

4.#validation
op=model.predict(xtest)
from sklearn.metrics import accuracy_score
score=accuracy_score(ytest,op)
print(score*100)

4.#predict...testing
op2=model.predict([[1.9,2.2,3.3,1.1]])
print(op2)




1.#Load the data
import pandas as pd
file=pd.read_csv("/irisexcel.csv")
x=file[["sepal_length","sepal_width","petal_length","petal_width"]]
y=file["species"]

2.#cross validation
from sklearn.model_selection import train_test_split
xtrain,xtest,ytrain,ytest=train_test_split(x,y,test_size=0.2)


3.#load the model
from sklearn.linear_model import Perceptron
model=Perceptron()

4.#fit into the model
model.fit(xtrain,ytrain)

4.#validation
op=model.predict(xtest)
from sklearn.metrics import accuracy_score
score=accuracy_score(ytest,op)
print(score*100)


4.#predict...testing
op2=model.predict([[1.9,2.2,3.3,1.1]])

print(op2)
from sklearn.metrics import confusion_matrix, ConfusionMatrixDisplay
cm=confusion_matrix(ytest,op)
print(cm)


cmd=ConfusionMatrixDisplay(confusion_matrix=cm,
                              display_labels=model.classes_)
cmd.plot()

1.#Load the data
import pandas as pd
file=pd.read_csv("/irisexcel.csv")
x=file[["sepal_length","sepal_width","petal_length","petal_width"]]
y=file["species"]

2.#cross validation
from sklearn.model_selection import train_test_split
xtrain,xtest,ytrain,ytest=train_test_split(x,y,test_size=0.2)


3.#load the model
from sklearn.linear_model import Perceptron
model=Perceptron()

4.#fit into the model
model.fit(xtrain,ytrain)

4.#validation
op=model.predict(xtest)
from sklearn.metrics import accuracy_score
score=accuracy_score(ytest,op)
print(score*100)


4.#predict...testing
op2=model.predict([[1.9,2.2,3.3,1.1]])

print(op2)
from sklearn.metrics import confusion_matrix, ConfusionMatrixDisplay
cm=confusion_matrix(ytest,op)
print(cm)


cmd=ConfusionMatrixDisplay(confusion_matrix=cm,
                              display_labels=model.classes_)
cmd.plot()


1.#Load the data
import pandas as pd
file=pd.read_csv("/irisexcel.csv")
x=file[["sepal_length","sepal_width","petal_length","petal_width"]]
y=file["species"]

2.#cross validation
from sklearn.model_selection import train_test_split
xtrain,xtest,ytrain,ytest=train_test_split(x,y,test_size=0.2)


3.#load the model
from sklearn.linear_model import Perceptron
model=Perceptron()

4.#fit into the model
model.fit(xtrain,ytrain)

4.#validation
op=model.predict(xtest)
from sklearn.metrics import accuracy_score
score=accuracy_score(ytest,op)
print(score*100)


4.#predict...testing
op2=model.predict([[1.9,2.2,3.3,1.1]])

print(op2)
from sklearn.metrics import confusion_matrix, ConfusionMatrixDisplay
cm=confusion_matrix(ytest,op)
print(cm)


cmd=ConfusionMatrixDisplay(confusion_matrix=cm,
                              display_labels=model.classes_)
cmd.plot()


import pandas as pd
file=pd.read_csv("/content/13 MLP winequality-red.csv")
print(file)





